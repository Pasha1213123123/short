class AppConstants {
  static const Duration controlsHideDuration = Duration(seconds: 3);

  static const Duration iconAnimationDuration = Duration(milliseconds: 500);

  static const Duration navigationDelay = Duration(milliseconds: 100);

  static const Duration snackBarDuration = Duration(milliseconds: 500);
}
